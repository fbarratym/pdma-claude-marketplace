# Run CENSO3 Tests

Run all unit tests for the CENSO3 solution.

## Steps:

1. Run xUnit tests in Tym.Censo3.Facade.Tests
2. Report test results including passed, failed, and skipped tests
3. If any tests fail, provide details about the failures

Use the following command:

```bash
cd C:\Users\fbarra.TYM\source\repos\CENSO3\Code
dotnet test CENSO3.sln --logger "console;verbosity=detailed"
```

Analyze the output and provide a summary of test results.
